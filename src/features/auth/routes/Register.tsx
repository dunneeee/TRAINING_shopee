import { AuthHeader, RegisterForm } from '../components';

const Register = () => {
  return (
    <section className="wrapper">
      <AuthHeader />
      <RegisterForm />
    </section>
  );
};

export default Register;
