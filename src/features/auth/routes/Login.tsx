import { AuthHeader, LoginForm } from '../components';

const Login = () => {
  return (
    <section className="wrapper">
      <AuthHeader />
      <LoginForm />
    </section>
  );
};

export default Login;
